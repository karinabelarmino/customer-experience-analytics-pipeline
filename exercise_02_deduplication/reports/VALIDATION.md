# Validation evidence — review candidate

Execution date: 2026-09-13. Portable runtime: Python 3.12.14. Spark runtime: PySpark 3.5.9,
local mode with two worker threads. All inputs are generated by this exercise.

| Gate | Observed result |
| --- | --- |
| `python -m unittest discover -s tests -v` | 21 tests passed |
| `python run_pipeline_local.py --demo` | Initial, incremental and replay jobs completed |
| Persistent incremental delivery vs. full retained-history rebuild | All four business datasets equal |
| Incremental vs. exact job replay | Same business-output SHA-256; retained Bronze stayed at 1,525 receipts |
| Referential integrity | SQLite keys and foreign keys enforced; foreign-key check passed |
| Receipt accounting | 1,215 + 1 + 153 + 125 + 31 = 1,525 |
| `python validate_spark.py` | Spark business datasets, receipt audit and quarantine reasons matched reference |
| Databricks managed Delta table writes and persistent views | Not executed; pending workspace validation |

The machine-readable output is in `execution_summary.json`, `job_runs.json` and
`spark_validation.json`. Spark parity compares complete rows, not only counts or aggregate metrics.
The audit CSV records the outcome for each receipt; rejected synthetic payloads are retained in
`quarantine.jsonl`.

The fixture's initial job has 1,217 active responses. Final active responses fall to 1,215 because
R012 receives a tombstone and BAD008 receives a contradictory reuse of its event ID. Both are
documented scenarios. Older valid versions and repeated transport deliveries are preserved in
Bronze, but do not increase the current response count.

The revised square figure uses only `survey-demo / R001–R003`, showing the eight problematic
joined rows. The corrected three-response dataset and its NPS arithmetic remain in the diagnostic
files and are independently asserted in `test_micro_example_fanout_changes_nps`.
Full-fixture diagnostics use the latest valid active snapshots before introducing the unsafe join,
so their 4,479 joined rows isolate child-cardinality multiplication.

The public scripts are freshly written. Original uploaded notebooks and their connection settings
are excluded. No GitHub mutation, release, tag, PR or social-media publication was performed.

Latest visual revision: the 1600 × 1600 result grid follows the author's supplied
SIGN-RESTRICTED BVAR graphic, with thin rounded outlines, subtle shadows and red emphasis. All eight displayed rows were read from the existing diagnostic
CSV; text bounds and cell widths were checked, and both language versions were visually inspected.
The previously approved post was preserved byte for byte. Pipeline code and data did not
change, so the recorded pipeline validation above remains applicable.
